# Frontend API Configuration
# Optional: Point to external FastAPI development backend (e.g., http://localhost:8000)
# Leave empty to use relative /api/chat proxied directly by the server
VITE_API_URL=http://localhost:8000
